import { Inter, Lexend } from "next/font/google";

const fontInter = Inter({
  weight: ["400", "500", "600", "700"],
  subsets: ["latin"],
});

const fontLexend = Lexend({
  weight: ["400", "500", "600", "700"],
  subsets: ["latin"],
});

export { fontInter, fontLexend };
